apiVersion: v1
kind: Service
metadata:
  name: {{ include "geo-app-chart.fullname" . }}
  labels:
    {{- include "geo-app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.container.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "geo-app-chart.selectorLabels" . | nindent 4 }}
